# MyApp - Node.js + Keycloak

Ứng dụng demo tích hợp Keycloak (OIDC) với custom theme, social login (Google + GitHub) và RBAC.

## Cài đặt
1. Copy file config:
   cp .env.example .env

2. Cài package:
   npm install

3. Chạy:
   npm start

Mở: http://localhost:3000
